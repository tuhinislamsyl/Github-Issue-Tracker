- Create a readme file and answer this question on your own. Don’t copy-paste from Google or any AI chatbot. 
    - 1️⃣ What is the difference between var, let, and const? 
      ANSWER : var let us call the variable even before it is not created that let and const don't, after we set a variable value with let we can re assign its value but we cannot do that in const  
    - 2️⃣ What is the spread operator (...)?
    ANSWER :  spread operator is three dots in JS (...) that let us spread the elements of array/object etc.
    - 3️⃣ What is the difference between map(), filter(), and forEach()?
    ANSWER : forEach() is just used for looping something it don't return anything and map loop through elements also return and creates an array filter is for looping through and select elements by condition 
    - 4️⃣ What is an arrow function?
    ANSWER : arrow function is a way to write shorted function in JS which was introduced in ES6 
    - 5️⃣ What are template literals?
    ANSWER : Template literals are also was introduced in ES6 by which we can create strings which is more readable and flexible compared to single or double quotes we can also set variables and expression directly inside a string using ${} .